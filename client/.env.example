# When running locally, keep empty (uses Vite dev proxy to localhost:5000)
# When deploying frontend to Vercel, set this to your Render backend URL:
# VITE_API_URL=https://room-competition-api.onrender.com
VITE_API_URL=
